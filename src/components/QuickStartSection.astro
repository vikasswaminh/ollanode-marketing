---
// src/components/QuickStartSection.astro
---

<section class="quick-start-orange-sec" id="start">
  <div class="quick-start-orange-banner">
    <div class="banner-inner-wrap">
      {/* LEFT COLUMN: ROCKET ICON & TEXT */}
      <div class="banner-left-content">
        <div class="rocket-icon-circle">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="rocket-svg">
            <path d="M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z"/>
            <path d="m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-3.05 11a22.35 22.35 0 0 1-3.95 2z"/>
            <path d="M9 12H4s.55-3.03 2-4c1.62-1.08 3 0 3 0"/>
            <path d="M12 15v5s3.03-.55 4-2c1.08-1.62 0-3 0-3"/>
          </svg>
        </div>
        <div class="banner-text-group">
          <h2 class="banner-title">Ready to take control of your infrastructure?</h2>
          <p class="banner-subtext">Join developers who trust OllaNode to build, scale and deliver without limits.</p>
        </div>
      </div>

      {/* RIGHT COLUMN: ACTION BUTTONS */}
      <div class="banner-ctas">
        <a href="https://login.ollanode.com" class="banner-btn-primary">
          GET STARTED
        </a>
        <a href="/docs" class="banner-btn-secondary">
          TALK TO US
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .quick-start-orange-sec {
    position: relative;
    width: 100%;
    background: radial-gradient(120% 180% at 50% 0%, rgba(99, 102, 241, 0.15) 0%, rgba(30, 27, 75, 0.45) 45%, #090D16 100%),
                linear-gradient(135deg, #090D16 0%, #0F172A 45%, #1E1B4B 85%, #0B0F17 100%);
    padding: 44px 0;
    overflow: hidden;
    border-top: 1px solid rgba(148, 163, 184, 0.2);
    border-bottom: 1px solid rgba(148, 163, 184, 0.2);
    box-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.08);
    z-index: 5;
  }

  .quick-start-orange-banner {
    width: 100%;
  }

  .banner-inner-wrap {
    width: 100%;
    max-width: var(--maxw, 1320px);
    margin: 0 auto;
    padding: 0 var(--gutter, clamp(20px, 4vw, 40px));
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    flex-wrap: wrap;
  }

  .banner-left-content {
    display: flex;
    align-items: center;
    gap: 20px;
    max-width: 820px;
  }

  .rocket-icon-circle {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: linear-gradient(135deg, #FF6A3D 0%, #FF4B24 50%, #D43210 100%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    display: grid;
    place-items: center;
    color: #FFFFFF;
    flex-shrink: 0;
    backdrop-filter: blur(4px);
    box-shadow: 0 4px 18px rgba(255, 75, 36, 0.4);
  }

  .rocket-svg {
    width: 26px;
    height: 26px;
  }

  .banner-text-group {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .banner-title {
    font-family: var(--sans);
    font-size: clamp(1.2rem, 1.8vw, 1.55rem);
    font-weight: 750;
    color: #FFFFFF;
    margin: 0;
    line-height: 1.25;
    letter-spacing: -0.015em;
  }

  .banner-subtext {
    font-family: var(--sans);
    font-size: clamp(0.88rem, 1vw, 0.98rem);
    font-weight: 450;
    color: #E2E8F0;
    margin: 0;
    line-height: 1.4;
    opacity: 1 !important;
    visibility: visible !important;
    display: block !important;
  }

  .banner-ctas {
    display: flex;
    align-items: center;
    gap: 14px;
    flex-wrap: wrap;
  }

  .banner-btn-primary {
    background: linear-gradient(135deg, #FF6A3D 0%, #FF4B24 45%, #D43210 100%);
    color: #FFFFFF;
    font-family: var(--sans);
    font-size: 13.5px;
    font-weight: 750;
    padding: 13px 26px;
    border-radius: 8px;
    border: 1px solid #D43210;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all 0.25s ease;
    box-shadow: 0 4px 16px rgba(255, 75, 36, 0.32);
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .banner-btn-primary:hover {
    background: linear-gradient(135deg, #FF7A4D 0%, #FF5A30 45%, #E23916 100%);
    border-color: #E23916;
    transform: translateY(-1.5px);
    box-shadow: 0 6px 22px rgba(255, 75, 36, 0.48);
    color: #FFFFFF;
  }

  .banner-btn-secondary {
    background: rgba(255, 255, 255, 0.05);
    color: #FFFFFF;
    border: 1.5px solid rgba(255, 255, 255, 0.22);
    font-family: var(--sans);
    font-size: 13.5px;
    font-weight: 750;
    padding: 13px 26px;
    border-radius: 8px;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all 0.25s ease;
    white-space: nowrap;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(4px);
  }

  .banner-btn-secondary:hover {
    background: rgba(255, 255, 255, 0.12);
    border-color: #FFFFFF;
    transform: translateY(-1.5px);
    color: #FFFFFF;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25);
  }

  @media (max-width: 900px) {
    .banner-inner-wrap {
      flex-direction: column;
      align-items: flex-start;
      gap: 20px;
    }

    .banner-ctas {
      width: 100%;
    }
  }

  @media (max-width: 640px) {
    .quick-start-orange-sec {
      padding: 32px 0;
    }

    .banner-left-content {
      gap: 14px;
    }

    .rocket-icon-circle {
      width: 44px;
      height: 44px;
    }

    .rocket-svg {
      width: 22px;
      height: 22px;
    }

    .banner-btn-primary,
    .banner-btn-secondary {
      width: 100%;
    }
  }
</style>
