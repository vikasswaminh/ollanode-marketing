---
import Site from '../../layouts/Site.astro';
import { getCollection, type CollectionEntry } from 'astro:content';

export async function getStaticPaths() {
  const articles = await getCollection('blog');
  return articles.map((article) => ({
    params: { slug: article.slug || article.id },
    props: { article },
  }));
}

interface Props {
  article: CollectionEntry<'blog'>;
}

const { article } = Astro.props;
const { Content, headings } = await article.render();

// Extract H2 & H3 headings for the "ON THIS PAGE" sticky left sidebar
const tocHeadings = headings.filter(
  (h) => (h.depth === 2 || h.depth === 3) && h.slug !== 'table-of-contents'
);

// Fetch related articles for the "RELATED READING" sticky right sidebar
const allArticles = await getCollection('blog');
const otherArticles = allArticles.filter((a) => a.id !== article.id);
const sameCategory = otherArticles.filter(
  (a) => a.data.category === article.data.category
);
const differentCategory = otherArticles.filter(
  (a) => a.data.category !== article.data.category
);
const relatedArticles = [...sameCategory, ...differentCategory].slice(0, 5);

// Key takeaways from frontmatter if available or defaults
const keyTakeaways = (article.data as any).keyTakeaways || [
  `<strong>OllaNode</strong> is a self-hosted video infrastructure platform built for developers who want control over their video stack.`,
  `It combines video processing, HLS delivery, CDN, storage, DNS, and edge functions in one platform.`,
  `The VOD pipeline is asynchronous and event-driven, covering transcoding, HLS generation, thumbnails, transcripts, storage, and webhooks.`,
  `<strong>Apache-2.0 licensing</strong> allows teams to inspect, modify, fork, and commercially deploy OllaNode.`,
  `<strong>AI-agent workflows</strong> include security guardrails, such as scoped capabilities, approval gates, audit logging, and human-only actions.`,
];
---

<Site
  title={`${article.data.title} · OllaNode Blog`}
  description={article.data.excerpt}
>
  <div class="article-page-wrap">
    <div class="container-wide">
      
      <!-- 3-COLUMN ARTICLE LAYOUT -->
      <div class="article-3col-layout">

        <!-- 1. LEFT SIDEBAR: STICKY "ON THIS PAGE" TABLE OF CONTENTS (IMAGE 1 STYLE) -->
        <aside class="sidebar-col sidebar-toc">
          <div class="sticky-sidebar">
            <span class="badge-pill founder-badge-purple">ON THIS PAGE</span>
            <nav class="toc-card-box" aria-label="Table of contents">
              {tocHeadings.length > 0 ? (
                <ul class="toc-list">
                  {tocHeadings.map((h) => (
                    <li class={`toc-item depth-${h.depth}`}>
                      <a href={`#${h.slug}`} class="toc-link" title={h.text}>
                        {h.text}
                      </a>
                    </li>
                  ))}
                </ul>
              ) : (
                <ul class="toc-list">
                  <li class="toc-item depth-2">
                    <a href="#overview" class="toc-link">Overview</a>
                  </li>
                  <li class="toc-item depth-2">
                    <a href="#architecture" class="toc-link">Architecture</a>
                  </li>
                </ul>
              )}
            </nav>
            <a href="/blog" class="view-all-btn-purple">← Back to Blog Home</a>
          </div>
        </aside>

        <!-- 2. MIDDLE COLUMN: MAIN ARTICLE CONTENT (IMAGE 1, 2, 3 LAYOUT & STYLING) -->
        <main class="article-middle-col">
          
          <!-- Breadcrumb Navigation (Exact Image 1 layout) -->
          <nav class="article-breadcrumb" aria-label="Breadcrumb">
            <a href="/" class="crumb-link">Home</a>
            <span class="crumb-sep">›</span>
            <a href="/blog" class="crumb-link">Blog</a>
            <span class="crumb-sep">›</span>
            <span class="crumb-current">{article.data.title}</span>
          </nav>

          <!-- Article Header (Exact Image 1 layout, fonts & spacing) -->
          <header class="article-header">
            <div class="article-meta-top">
              <span class="category-pill-orange">{article.data.category || 'Product & Changelog'}</span>
              <span class="meta-dot">·</span>
              <span class="meta-item">{article.data.publishedDate || 'Aug 21, 2026'}</span>
              <span class="meta-dot">·</span>
              <span class="meta-item">{article.data.readingTime || '27 min read'}</span>
            </div>

            <h1 class="article-main-title">{article.data.title}</h1>

            <p class="article-lead-desc">{article.data.excerpt}</p>

            <!-- Exact single-line author credit matching Image 1 -->
            <div class="article-byline-bar">
              <span>By <strong class="byline-highlight">{article.data.author?.name || 'The OllaNode Team'}</strong></span>
              <span class="byline-sep">·</span>
              <span>Category: <strong class="byline-highlight">{article.data.category || 'Product & Changelog'}</strong></span>
              <span class="byline-sep">·</span>
              <span>Published {article.data.publishedDate || 'Aug 21, 2026'}</span>
            </div>
          </header>

          <!-- Optional Hero Banner Image -->
          {article.data.image && (
            <div class="article-hero-banner">
              <img src={article.data.image} alt={article.data.title} />
            </div>
          )}

          <!-- Key Takeaways Callout Box (Exact Image 2 layout & styling) -->
          {keyTakeaways && keyTakeaways.length > 0 && (
            <div class="key-takeaways-box">
              <div class="takeaways-header">
                <span class="takeaways-sparkle-icon" aria-hidden="true">✦</span>
                <h3 class="takeaways-title">KEY TAKEAWAYS</h3>
              </div>
              <ul class="takeaways-list">
                {keyTakeaways.map((item: string) => (
                  <li set:html={item} />
                ))}
              </ul>
            </div>
          )}

          <!-- Main Article Prose (Exact Image 3 Heading & Paragraph Typography) -->
          <div class="article-prose-content prose">
            <Content />
          </div>

          <!-- Tags Footer -->
          {article.data.tags && article.data.tags.length > 0 && (
            <div class="article-tags-wrap">
              <span class="tags-label">Tags:</span>
              <div class="tags-list">
                {article.data.tags.map((tag) => (
                  <span class="tag-chip">#{tag}</span>
                ))}
              </div>
            </div>
          )}

          <!-- Bottom CTA Banner Card -->
          <div class="article-bottom-cta">
            <div class="bottom-cta-inner">
              <span class="cta-badge">GET STARTED</span>
              <h3 class="cta-title">Run your own VOD transcoding, adaptive HLS, edge CDN, and AI-governed media storage</h3>
              <p class="cta-desc">Deploy Apache-2.0 open source video infrastructure on your own cloud hardware in minutes.</p>
              <div class="cta-btn-wrap">
                <a href="https://ollanode.com" class="btn-cta-orange">Explore OllaNode ↗</a>
              </div>
            </div>
          </div>

        </main>

        <!-- 3. RIGHT SIDEBAR: STICKY "RELATED READING" CARDS (IMAGE 1 STYLE) -->
        <aside class="sidebar-col sidebar-related">
          <div class="sticky-sidebar">
            <span class="badge-pill orange-badge">RELATED READING</span>
            <div class="related-cards-stack">
              {relatedArticles.map((art) => (
                <a href={`/blog/${art.slug || art.id}`} class="related-card-item">
                  <span class="related-card-category">{art.data.category} ↗</span>
                  <h4 class="related-card-title">{art.data.title}</h4>
                  <p class="related-card-desc">{art.data.excerpt}</p>
                </a>
              ))}
            </div>
          </div>
        </aside>

      </div>
    </div>
  </div>
</Site>

<!-- CLIENT SCRIPT FOR TOC ACTIVE STATE TRACKING -->
<script is:inline>
  (function () {
    function initTocTracker() {
      var links = document.querySelectorAll('.toc-link');
      if (!links.length) return;

      var headings = [];
      links.forEach(function (link) {
        var href = link.getAttribute('href');
        if (href && href.startsWith('#')) {
          var el = document.getElementById(href.substring(1));
          if (el) {
            headings.push({ el: el, link: link });
          }
        }
      });

      function onScroll() {
        var scrollPos = window.scrollY + 140;
        var active = null;

        for (var i = 0; i < headings.length; i++) {
          if (headings[i].el.offsetTop <= scrollPos) {
            active = headings[i];
          } else {
            break;
          }
        }

        links.forEach(function (l) {
          l.classList.remove('active');
        });

        if (active) {
          active.link.classList.add('active');
        } else if (links.length > 0) {
          links[0].classList.add('active');
        }
      }

      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initTocTracker);
    } else {
      initTocTracker();
    }

    document.addEventListener('astro:page-load', initTocTracker);
  })();
</script>

<style>
  /* ==========================================================================
     EXACT REPLICATION OF BLOGS.OLLANODE.COM POST LAYOUT & STYLING
     (MATCHING IMAGES 1, 2, AND 3)
     ========================================================================== */

  .article-page-wrap {
    width: 100%;
    background-color: #FFFFFF;
    color: #0F172A;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    padding: 2.25rem 0 5rem;
    position: relative;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  .container-wide {
    width: 100%;
    max-width: 86rem;
    margin: 0 auto;
    padding: 0 1.25rem;
    box-sizing: border-box;
  }

  /* 3-COLUMN GRID LAYOUT (EXACT IMAGE 1 PROPORTIONS) */
  .article-3col-layout {
    display: grid;
    grid-template-columns: 240px minmax(0, 1fr) 240px;
    gap: 2.5rem;
    width: 100%;
    position: relative;
    align-items: start;
  }

  /* SIDEBAR COMMON STYLES */
  .sidebar-col {
    position: -webkit-sticky !important;
    position: sticky !important;
    top: 86px !important;
    align-self: start !important;
    z-index: 25 !important;
    width: 100% !important;
  }

  .sticky-sidebar {
    display: flex !important;
    flex-direction: column !important;
    gap: 0.85rem !important;
  }

  /* BADGE PILLS (EXACT IMAGE 1) */
  .badge-pill {
    display: inline-flex;
    align-items: center;
    align-self: flex-start;
    width: max-content;
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.05em;
    padding: 0.35rem 0.85rem;
    border-radius: 9999px;
    text-transform: uppercase;
    white-space: nowrap;
    font-family: 'Inter', sans-serif;
  }

  .founder-badge-purple {
    background: #7C3AED;
    color: #FFFFFF;
    box-shadow: 0 2px 8px rgba(124, 58, 237, 0.22);
  }

  .orange-badge {
    background: #FF6600;
    color: #FFFFFF;
    box-shadow: 0 2px 8px rgba(255, 102, 0, 0.22);
  }

  /* LEFT SIDEBAR: TABLE OF CONTENTS BOX (EXACT IMAGE 1) */
  .toc-card-box {
    background: #FAFAFA;
    border: 1.5px solid #E2E8F0;
    border-radius: 12px;
    padding: 0.85rem 0.65rem;
    box-sizing: border-box;
    max-height: calc(100vh - 180px);
    overflow-y: auto;
  }

  /* Custom subtle scrollbar */
  .toc-card-box::-webkit-scrollbar {
    width: 5px;
  }
  .toc-card-box::-webkit-scrollbar-track {
    background: transparent;
  }
  .toc-card-box::-webkit-scrollbar-thumb {
    background: #CBD5E1;
    border-radius: 4px;
  }

  .toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .toc-item {
    line-height: 1.35;
  }

  .toc-item.depth-3 {
    padding-left: 0.75rem;
  }

  .toc-link {
    display: block;
    font-size: 0.81rem;
    font-weight: 500;
    color: #475569;
    text-decoration: none;
    padding: 0.32rem 0.5rem;
    border-radius: 6px;
    border-left: 2px solid transparent;
    transition: all 0.15s ease;
    white-space: normal;
    word-break: break-word;
    line-height: 1.32;
  }

  .toc-link:hover {
    color: #7C3AED;
    background: #F3E8FF;
    border-left-color: #7C3AED;
  }

  .toc-link.active {
    color: #7C3AED;
    font-weight: 700;
    background: #F3E8FF;
    border-left-color: #7C3AED;
  }

  .view-all-btn-purple {
    display: inline-flex;
    align-items: center;
    font-size: 0.84rem;
    font-weight: 700;
    color: #7C3AED;
    text-decoration: none;
    transition: all 0.15s ease;
    padding: 0.25rem 0;
  }

  .view-all-btn-purple:hover {
    color: #6D28D9;
    text-decoration: underline;
    transform: translateX(-2px);
  }

  /* MIDDLE COLUMN */
  .article-middle-col {
    min-width: 0;
    width: 100%;
  }

  /* BREADCRUMB (IMAGE 1) */
  .article-breadcrumb {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.45rem;
    font-size: 0.82rem;
    color: #64748B;
    margin-bottom: 1.25rem;
  }

  .crumb-link {
    color: #64748B;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.15s ease;
  }

  .crumb-link:hover {
    color: #FF6600;
    text-decoration: underline;
  }

  .crumb-sep {
    color: #94A3B8;
    font-size: 0.9rem;
  }

  .crumb-current {
    color: #0F172A;
    font-weight: 600;
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
    max-width: 480px;
  }

  /* ARTICLE HEADER (IMAGE 1) */
  .article-header {
    margin-bottom: 1.75rem;
  }

  .article-meta-top {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
    font-size: 0.84rem;
    color: #64748B;
    font-weight: 500;
    margin-bottom: 0.95rem;
  }

  .category-pill-orange {
    display: inline-block;
    background: #FFF7ED;
    color: #EA580C;
    border: 1px solid #FFEDD5;
    font-size: 0.72rem;
    font-weight: 800;
    padding: 0.22rem 0.7rem;
    border-radius: 9999px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .meta-dot {
    color: #94A3B8;
    font-size: 0.95rem;
    line-height: 1;
  }

  .meta-item {
    color: #64748B;
  }

  /* H1 TITLE (IMAGE 1) */
  .article-main-title {
    font-size: 2.35rem;
    font-weight: 850;
    color: #0F172A;
    line-height: 1.2;
    letter-spacing: -0.03em;
    margin: 0 0 1.15rem;
  }

  /* SUBTITLE / LEAD (IMAGE 1) */
  .article-lead-desc {
    font-size: 1.1rem;
    color: #475569;
    line-height: 1.6;
    margin: 0 0 1.35rem;
  }

  /* SINGLE-LINE AUTHOR BYLINE BAR (IMAGE 1) */
  .article-byline-bar {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.45rem;
    font-size: 0.84rem;
    color: #64748B;
    padding-bottom: 1.25rem;
    border-bottom: 1px solid #E2E8F0;
  }

  .byline-highlight {
    color: #0F172A;
    font-weight: 700;
  }

  .byline-sep {
    color: #94A3B8;
  }

  .article-hero-banner {
    margin-bottom: 2rem;
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid #E2E8F0;
  }

  .article-hero-banner img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* ==========================================================================
     KEY TAKEAWAYS BOX (EXACT REPLICATION OF IMAGE 2)
     ========================================================================== */
  .key-takeaways-box {
    background: #FFFDF9;
    border-left: 4.5px solid #FF5500;
    border-radius: 0 14px 14px 0;
    padding: 1.4rem 1.75rem;
    margin: 2rem 0 2.5rem;
    box-shadow: 0 2px 10px rgba(255, 85, 0, 0.04);
  }

  .takeaways-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.95rem;
  }

  .takeaways-sparkle-icon {
    color: #FF5500;
    font-size: 1rem;
    line-height: 1;
    display: inline-block;
  }

  .takeaways-title {
    font-size: 0.95rem;
    font-weight: 850;
    color: #FF5500;
    margin: 0;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-family: 'Inter', sans-serif;
  }

  .takeaways-list {
    margin: 0;
    padding-left: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    list-style-type: disc;
  }

  .takeaways-list li {
    font-size: 0.96rem;
    color: #1E293B;
    line-height: 1.62;
  }

  .takeaways-list li :global(strong) {
    color: #0F172A;
    font-weight: 750;
  }

  /* ==========================================================================
     ARTICLE PROSE TYPOGRAPHY & HEADING SIZES (EXACT REPLICATION OF IMAGE 3)
     ========================================================================== */
  .article-prose-content {
    font-size: 1.05rem;
    line-height: 1.75;
    color: #1E293B;
  }

  /* H2 HEADING (EXACT IMAGE 3) */
  .article-prose-content h2 {
    font-size: 1.75rem;
    font-weight: 800;
    color: #0F172A;
    margin: 2.75rem 0 1.15rem;
    line-height: 1.25;
    letter-spacing: -0.025em;
    scroll-margin-top: 96px;
  }

  /* H3 SUBHEADING (EXACT IMAGE 3) */
  .article-prose-content h3 {
    font-size: 1.35rem;
    font-weight: 750;
    color: #0F172A;
    margin: 2.1rem 0 0.85rem;
    line-height: 1.32;
    letter-spacing: -0.015em;
    scroll-margin-top: 96px;
  }

  /* H4 SUBHEADING */
  .article-prose-content h4 {
    font-size: 1.15rem;
    font-weight: 700;
    color: #0F172A;
    margin: 1.6rem 0 0.65rem;
    line-height: 1.35;
  }

  .article-prose-content p {
    margin: 0 0 1.25rem;
    color: #334155;
    line-height: 1.75;
  }

  .article-prose-content a {
    color: #FF6600;
    text-decoration: underline;
    text-underline-offset: 3px;
    font-weight: 600;
    transition: color 0.15s ease;
  }

  .article-prose-content a:hover {
    color: #E65100;
  }

  /* NUMBERED & BULLETED LISTS (EXACT IMAGE 3) */
  .article-prose-content ol,
  .article-prose-content ul {
    margin: 0 0 1.5rem;
    padding-left: 1.4rem;
  }

  .article-prose-content li {
    margin-bottom: 0.75rem;
    line-height: 1.7;
    color: #334155;
  }

  .article-prose-content li strong {
    color: #0F172A;
    font-weight: 700;
  }

  .article-prose-content pre {
    background: #0B0F19 !important;
    color: #F8FAFC !important;
    padding: 1.15rem 1.4rem !important;
    border-radius: 12px !important;
    overflow-x: auto !important;
    font-family: 'JetBrains Mono', 'Fira Code', monospace !important;
    font-size: 0.88rem !important;
    line-height: 1.6 !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    margin: 1.5rem 0 1.75rem !important;
  }

  .article-prose-content code {
    background: #F1F5F9;
    color: #0F172A;
    padding: 0.15rem 0.45rem;
    border-radius: 6px;
    font-size: 0.88em;
    font-family: 'JetBrains Mono', 'Fira Code', monospace;
    border: 1px solid #E2E8F0;
  }

  .article-prose-content pre code {
    background: transparent !important;
    color: inherit !important;
    padding: 0 !important;
    border: none !important;
  }

  .article-prose-content blockquote {
    border-left: 4px solid #FF6600;
    background: #FFF7ED;
    padding: 1rem 1.35rem;
    margin: 1.5rem 0;
    border-radius: 0 10px 10px 0;
    color: #431407;
    font-style: italic;
  }

  .article-prose-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1.75rem 0;
    font-size: 0.92rem;
  }

  .article-prose-content th,
  .article-prose-content td {
    padding: 0.75rem 1rem;
    border: 1px solid #E2E8F0;
    text-align: left;
  }

  .article-prose-content th {
    background: #F8FAFC;
    font-weight: 700;
    color: #0F172A;
  }

  /* TAGS FOOTER */
  .article-tags-wrap {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.6rem;
    padding: 1.5rem 0;
    margin-top: 2rem;
    border-top: 1px solid #E2E8F0;
  }

  .tags-label {
    font-size: 0.88rem;
    font-weight: 700;
    color: #64748B;
  }

  .tags-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .tag-chip {
    display: inline-block;
    background: #F1F5F9;
    color: #334155;
    font-size: 0.78rem;
    font-weight: 600;
    padding: 0.25rem 0.65rem;
    border-radius: 9999px;
    border: 1px solid #E2E8F0;
  }

  /* BOTTOM CTA BANNER CARD */
  .article-bottom-cta {
    margin-top: 2.5rem;
    background: linear-gradient(135deg, #0F172A 0%, #080C14 100%);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 18px;
    padding: 2.25rem 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    box-sizing: border-box;
  }

  .bottom-cta-inner {
    max-width: 680px;
  }

  .cta-badge {
    display: inline-block;
    background: #FF6600;
    color: #FFFFFF;
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.05em;
    padding: 0.22rem 0.65rem;
    border-radius: 9999px;
    text-transform: uppercase;
    margin-bottom: 0.85rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .cta-title {
    font-size: 1.35rem;
    font-weight: 800;
    color: #FFFFFF;
    line-height: 1.3;
    margin: 0 0 0.65rem;
    letter-spacing: -0.015em;
  }

  .cta-desc {
    font-size: 0.92rem;
    color: #94A3B8;
    line-height: 1.5;
    margin: 0 0 1.35rem;
  }

  .btn-cta-orange {
    display: inline-flex;
    align-items: center;
    background: linear-gradient(135deg, #FF6600, #E65100);
    color: #FFFFFF !important;
    font-size: 0.92rem;
    font-weight: 700;
    padding: 0.75rem 1.6rem;
    border-radius: 10px;
    border: 1px solid #FDE047;
    text-decoration: none !important;
    transition: all 0.2s ease;
    box-shadow: 0 4px 16px rgba(255, 102, 0, 0.35);
  }

  .btn-cta-orange:hover {
    background: #E65100;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(255, 102, 0, 0.45);
  }

  /* RIGHT SIDEBAR: RELATED READING (IMAGE 1) */
  .related-cards-stack {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .related-card-item {
    background: #FFFFFF;
    border: 1.5px solid #E2E8F0;
    border-radius: 12px;
    padding: 0.85rem 0.95rem;
    display: flex;
    flex-direction: column;
    text-decoration: none !important;
    transition: all 0.2s ease;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
  }

  .related-card-item:hover {
    border-color: #FF6600;
    transform: translateY(-2px);
    box-shadow: 0 6px 16px -2px rgba(255, 102, 0, 0.2);
  }

  .related-card-category {
    font-size: 0.66rem;
    font-weight: 800;
    color: #EA580C;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    margin-bottom: 0.35rem;
  }

  .related-card-title {
    font-size: 0.84rem;
    font-weight: 700;
    color: #0F172A;
    line-height: 1.32;
    margin: 0 0 0.35rem;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    transition: color 0.15s ease;
  }

  .related-card-item:hover .related-card-title {
    color: #FF6600;
  }

  .related-card-desc {
    font-size: 0.74rem;
    color: #64748B;
    line-height: 1.4;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* ==========================================================================
     RESPONSIVE BREAKPOINTS
     ========================================================================== */

  /* LAPTOP / SMALL DESKTOP (901px to 1180px) */
  @media (min-width: 901px) and (max-width: 1180px) {
    .article-3col-layout {
      grid-template-columns: 190px minmax(0, 1fr) 200px;
      gap: 1.5rem;
    }
    .article-main-title {
      font-size: 1.95rem;
    }
  }

  /* TABLET (768px to 900px) */
  @media (min-width: 768px) and (max-width: 900px) {
    .article-3col-layout {
      grid-template-columns: 150px minmax(0, 1fr) 160px;
      gap: 1rem;
    }
    .article-main-title {
      font-size: 1.65rem;
    }
    .founder-badge-purple,
    .orange-badge {
      font-size: 0.62rem;
      padding: 0.22rem 0.5rem;
    }
    .toc-link {
      font-size: 0.74rem;
      padding: 0.25rem 0.35rem;
    }
    .related-card-title {
      font-size: 0.78rem;
    }
    .related-card-desc {
      font-size: 0.7rem;
    }
  }

  /* MOBILE (<= 767px) */
  @media (max-width: 767px) {
    .article-page-wrap {
      padding: 1.25rem 0 3.5rem;
    }
    .article-3col-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .sidebar-toc {
      display: none !important;
    }
    .sidebar-related {
      position: static !important;
      margin-top: 1.5rem;
    }
    .article-main-title {
      font-size: 1.6rem;
      line-height: 1.25;
    }
    .article-lead-desc {
      font-size: 0.96rem;
    }
    .key-takeaways-box {
      padding: 1.15rem 1.25rem;
      border-left-width: 4px;
    }
    .article-bottom-cta {
      padding: 1.5rem 1.25rem;
    }
    .cta-title {
      font-size: 1.15rem;
    }
  }
</style>
